# SafeTrip B2B 포털 와이어프레임

| 항목 | 내용 |
|------|------|
| 문서 ID | WF-B2B-N |
| 계층 | 와이어프레임 (화면구성원칙 하위) |
| 버전 | v2.0 |
| 작성일 | 2026-03-04 |
| 기준 문서 | 비즈니스원칙 v5.1 §01.4·§12, 14_온보딩_UX_시나리오 §7(시나리오 E), 23_초대코드_원칙 §09 |

---

## 1. 개요

- **화면 수:** 3개 (N-01 ~ N-03)
- **Phase:** 전체 P3
- **핵심 역할:** B2B 관리자 (학교/여행사/기업)
- **구현 형태:** 웹 앱 (모바일 반응형 뷰 포함)
- **B2B 액센트:** `#1565C0` (dark blue)

### 1.1 Design Tokens (B2B 전용)

| 토큰명 | HEX | 용도 |
|--------|-----|------|
| `b2bAccent` | `#1565C0` | B2B 포털 주요 액센트 색상, AppBar 배경, CTA 버튼 |
| `b2bAccentLight` | `#1E88E5` | 호버 상태, 보조 강조 |
| `b2bAccentDark` | `#0D47A1` | 프레스 상태, 헤더 텍스트 |
| `b2bSurface` | `#F5F7FA` | 대시보드 배경 |
| `b2bCardBg` | `#FFFFFF` | 카드 배경 (elevation 2) |

> B2B 포털은 일반 앱의 `primaryTeal` 대신 `b2bAccent` (`#1565C0`)를 주요 색상으로 사용한다.
> 나머지 시맨틱 컬러(success, warning, error)는 글로벌 스타일 가이드와 동일하다.

### 1.2 B2B 역할 자동 매핑 (비즈니스원칙 §01.4)

B2B CSV 일괄 등록 시, 고객 유형에 따라 SafeTrip 내부 역할이 자동 매핑된다.
표시명은 앱 UI 레이어에서 변환하며, DB에는 항상 내부 역할 코드를 저장한다.

| B2B 고객 유형 | 캡틴 | 크루장 | 크루 | 가디언 |
|-------------|------|-------|------|--------|
| 학교 (수학여행) | 인솔 교사 | 부담임/보조 교사 | 학생 | 학부모/학년 부장 |
| 여행사 (패키지) | 여행 가이드 | 보조 가이드 | 참가자 | 참가자 가족 |
| 기업 (출장) | 팀장/관리자 | 부팀장 | 팀원 | 팀원 가족/회사 안전담당자 |

### 1.3 User Journey Flow

```
[B2B 관리자 로그인]
       ↓
N-01 B2B 대시보드
 ├── [대량 여행 생성] → N-02 대량 여행 생성
 └── [대량 초대]     → N-03 대량 초대 코드
                       ├── 코드 일괄 생성 (CSV)
                       ├── SMS/이메일 발송
                       └── 코드 상태 대시보드
```

### 1.4 외부 진입/이탈 참조

| 출발 | 조건 | 도착 | 카테고리 |
|------|------|------|---------|
| 로그인 | B2B 관리자 인증 완료 | N-01 B2B 대시보드 | N |
| N-01 | 대량 여행 생성 버튼 | N-02 대량 여행 생성 | N |
| N-01 | 대량 초대 버튼 | N-03 대량 초대 코드 | N |
| N-02 | 생성 완료 | N-01 B2B 대시보드 | N |
| N-03 | 발송 완료 | N-01 B2B 대시보드 | N |

---

## 2. 화면 정의

---

### 2.1 [N-01] B2B 대시보드

#### 메타데이터

| 항목 | 값 |
|------|-----|
| ID | N-01 |
| 화면명 | B2B 대시보드 (B2B Dashboard) |
| Phase | P3 |
| 역할 | B2B 관리자 |
| 진입 경로 | B2B 로그인 완료 → N-01 |
| 이탈 경로 | N-01 → N-02 (대량 여행 생성), N-01 → N-03 (대량 초대 코드) |

#### 레이아웃

```
┌─────────────────────────────────────┐
│ [☰]  SafeTrip B2B       [🔔] [👤]  │ AppBar (b2bAccent #1565C0)
├─────────────────────────────────────┤
│                                     │
│  ┌──────────┐  조직명               │ Organization header
│  │  Logo    │  학교/여행사/기업       │ 조직 로고 (48x48) + 조직명 + 유형
│  └──────────┘                       │
│                                     │
│  ┌────────┐ ┌────────┐              │ Statistics Cards (2x2 grid)
│  │ 📊     │ │ 👥     │              │
│  │ 활성    │ │ 전체   │              │ Card_Standard, elevation 2
│  │ 여행    │ │ 멤버   │              │
│  │  12    │ │  348   │              │ headlineMedium (값)
│  └────────┘ └────────┘              │ bodySmall (라벨)
│  ┌────────┐ ┌────────┐              │
│  │ ✉️     │ │ ✅     │              │
│  │ 초대   │ │ 코드   │              │
│  │ 발송   │ │ 사용률  │              │
│  │  520   │ │ 78.3%  │              │
│  └────────┘ └────────┘              │
│                                     │
│  ── 초대코드 상태 현황 ─────────────  │ Section header (titleMedium)
│  ┌─────────────────────────────┐    │
│  │ 전체: 520건                  │    │ 요약 바
│  │ ███████░░░░░░░░             │    │ SegmentedProgressBar
│  │ 📨 발송됨 120  👁️ 수신확인 85 │    │ (blue/teal/green/gray)
│  │ ✅ 사용됨 280  ⏳ 만료됨 35  │    │
│  └─────────────────────────────┘    │
│                                     │
│  ── 최근 여행 ──────────────────    │ Section header (titleMedium)
│  ┌─────────────────────────────┐    │
│  │ # │ 여행명      │ 기간   │상태│    │ DataTable (compact)
│  ├─────────────────────────────┤    │
│  │ 1 │ 제주수학여행 │ 3/15-18│ 활성│    │
│  │ 2 │ 부산체험학습 │ 3/20-22│ 예정│    │
│  │ 3 │ 도쿄연수    │ 4/01-05│ 예정│    │
│  └─────────────────────────────┘    │
│                                     │
│  ── 빠른 실행 ────────────────       │ Section header (titleMedium)
│  ┌─────────────────┐ ┌─────────────────┐
│  │ 📋 대량 여행 생성 │ │ ✉️ 대량 초대 코드 │
│  └─────────────────┘ └─────────────────┘
│                                     │
└─────────────────────────────────────┘
```

#### 컴포넌트 명세

| 컴포넌트 | Flutter 위젯 | 속성 |
|----------|-------------|------|
| 앱바 | `AppBar` | backgroundColor: b2bAccent (#1565C0), leading: 햄버거 메뉴, title: "SafeTrip B2B", actions: [알림, 프로필] |
| 조직 헤더 | `Row` | leading: `CircleAvatar` (48dp, 조직 로고), title: 조직명 (`titleLarge`), subtitle: 조직 유형 (`bodySmall`) |
| 통계 카드 (4개) | `Card` | style: Card_Standard, elevation: 2, radius16, 2x2 `GridView`, 아이콘 + 수치 (`headlineMedium`) + 라벨 (`bodySmall`) |
| 초대코드 상태 요약 | `Card` + `SegmentedProgressBar` | 4색 세그먼트: b2bAccent (발송됨) / primaryTeal (수신확인) / semanticSuccess (사용됨) / onSurfaceVariant (만료됨) |
| 최근 여행 테이블 | `DataTable` | columns: [#, 여행명, 기간, 상태], compact, 최대 5행 + "더 보기" |
| 빠른 실행 버튼 (2개) | `Card` + `InkWell` | style: Card_Standard, elevation: 1, 아이콘 (32dp) + 라벨 (`labelMedium`), 1x2 `GridView` |
| 섹션 헤더 | `Text` | style: titleMedium (18sp, SemiBold), color: onSurface |

#### 역할별 분기

해당 없음 (B2B 포털은 B2B 관리자 단일 역할).

#### 여행상태별 분기

해당 없음 (B2B 포털은 여행 상태에 무관하게 동일 동작).

#### 프라이버시등급별 분기

해당 없음 (B2B 포털은 프라이버시 등급에 무관하게 동일 동작).

#### 상태 분기

| 상태 | UI 변화 |
|------|---------|
| 초기 로딩 | 통계 카드 4개 Shimmer 효과, 테이블/차트 영역 Skeleton 표시 |
| 데이터 로드 완료 | 통계 카드 수치 표시, 초대코드 상태 바 렌더링 |
| 활성 여행 0 | "활성 여행 없음" 안내 + "새 여행 만들기" CTA 표시 |
| 초대코드 발송 0 | 초대코드 상태 현황 "아직 발송된 초대코드가 없습니다" 안내 |
| 코드 사용률 100% | 코드 사용률 카드 semanticSuccess 색상 + "전원 참여 완료" 라벨 |
| 만료 코드 존재 | 만료됨 카운트 secondaryAmber 색상으로 강조 |
| 네트워크 오류 | SnackBar "데이터를 불러올 수 없습니다. 다시 시도해주세요." |

#### 인터랙션

- [탭] 햄버거 메뉴 (☰) → Drawer 사이드바 (N-02, N-03 메뉴 목록 + 로그아웃)
- [탭] 통계 카드 (초대 발송) → N-03 대량 초대 코드 (상태 탭 활성)
- [탭] 통계 카드 (코드 사용률) → N-03 대량 초대 코드 (상태 탭 활성)
- [탭] 초대코드 상태 현황 카드 → N-03 대량 초대 코드 (코드 상태 대시보드 섹션)
- [탭] 최근 여행 행 → 해당 여행 상세 Modal_Bottom (멤버 수, 기간, 초대 현황)
- [탭] 빠른 실행 "대량 여행 생성" → N-02 대량 여행 생성
- [탭] 빠른 실행 "대량 초대 코드" → N-03 대량 초대 코드
- [탭] 알림 아이콘 → 알림 목록 Modal_Bottom (코드 만료 임박, 배치 완료 등)
- [탭] 프로필 아이콘 → 관리자 설정 / 로그아웃

---

### 2.2 [N-02] 대량 여행 생성

#### 메타데이터

| 항목 | 값 |
|------|-----|
| ID | N-02 |
| 화면명 | 대량 여행 생성 (Trip Bulk Create) |
| Phase | P3 |
| 역할 | B2B 관리자 |
| 진입 경로 | N-01 대시보드 → 빠른 실행 / 사이드바 → N-02 |
| 이탈 경로 | N-02 → N-01 (생성 완료 시) |

#### 레이아웃

```
┌─────────────────────────────────────┐
│ [←] 대량 여행 생성           [?]    │ AppBar (b2bAccent)
├─────────────────────────────────────┤
│                                     │
│  CSV 파일로 여행을                   │ headlineMedium (24sp, SemiBold)
│  일괄 생성합니다                     │
│                                     │
│  ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐ │
│  ╎                               ╎ │ Dropzone (dotted border)
│  ╎       📁                      ╎ │ b2bAccent icon (48dp)
│  ╎                               ╎ │
│  ╎   CSV 파일을 끌어다 놓거나     ╎ │ bodyMedium, onSurfaceVariant
│  ╎   탭하여 파일을 선택하세요     ╎ │
│  ╎                               ╎ │
│  ╎   [📥 샘플 CSV 다운로드]       ╎ │ TextButton, b2bAccent
│  ╎                               ╎ │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘ │
│                                     │
│  ── 여행 기간 제한 안내 ─────────    │ Card_Alert (amber border)
│  ┌─────────────────────────────┐    │
│  │ ⚠️ 여행 기간은 최대 15일입니다. │    │ bodyMedium, secondaryAmber
│  │ 15일 초과 여행은 자동으로      │    │
│  │ 분할 안내됩니다. (§02.3)      │    │
│  └─────────────────────────────┘    │
│                                     │
│  ── 업로드 상태 ─────────────────    │
│  ┌─────────────────────────────┐    │
│  │ trips_2026_spring.csv       │    │ 파일명 + 크기
│  │ ████████████░░░  78%        │    │ LinearProgressIndicator
│  │ 150건 파싱 중...             │    │ bodySmall, onSurfaceVariant
│  └─────────────────────────────┘    │
│                                     │
│  ── 검증 결과 ───────────────────    │ Section header
│  ┌─────────────────────────────┐    │
│  │ # │ 여행명    │ 기간    │상태│    │ DataTable header
│  ├─────────────────────────────┤    │
│  │ 1 │ 제주수학..│ 3/15-18│ ✅ │    │ 성공 행 (semanticSuccess)
│  │ 2 │ 부산체험..│ 3/20-22│ ✅ │    │
│  │ 3 │ 도쿄연수..│ invalid│ ❌ │    │ 오류 행 (semanticError bg)
│  │   │ → 날짜 형식 오류       │    │ 에러 메시지 (bodySmall, red)
│  │ 4 │ 오사카..  │ 16일   │ ❌ │    │ 15일 초과 오류
│  │   │ → 여행 기간 15일 초과   │    │
│  │...│          │        │    │    │
│  └─────────────────────────────┘    │
│  성공: 146건 / 오류: 4건            │ bodyMedium, 색상 분기
│                                     │
│  [📥 오류 리포트 다운로드]           │ TextButton, semanticError
│                                     │
│  ┌─────────────────────────────┐    │
│  │    일괄 생성 (146건)          │    │ Button_Primary (b2bAccent)
│  └─────────────────────────────┘    │
│                                     │
└─────────────────────────────────────┘
```

#### 컴포넌트 명세

| 컴포넌트 | Flutter 위젯 | 속성 |
|----------|-------------|------|
| 앱바 | `AppBar` | backgroundColor: b2bAccent (#1565C0), title: "대량 여행 생성", leading: BackButton, actions: [도움말 아이콘] |
| 제목 | `Text` | style: headlineMedium (24sp, SemiBold), color: onSurface |
| 드롭존 | `DottedBorder` + `GestureDetector` | border: dashed 2px b2bAccent (#1565C0), radius16, 배경: b2bAccent 5% opacity, 높이: 180dp |
| 파일 아이콘 | `Icon` | Icons.cloud_upload, size: 48, color: b2bAccent (#1565C0) |
| 샘플 다운로드 | `TextButton` | icon: Icons.download, text: "샘플 CSV 다운로드", color: b2bAccent |
| 여행 기간 안내 | `Card` | style: Card_Alert, 좌측 보더 4px secondaryAmber, 아이콘: warning, 15일 제한 안내 텍스트 |
| 업로드 진행 | `Card` + `LinearProgressIndicator` | Card_Standard, progressColor: b2bAccent (#1565C0), trackColor: outline (#EDEDED) |
| 파일 정보 | `Text` | 파일명 (`bodyLarge`), 파일 크기 (`bodySmall`), 진행률 (`bodySmall`) |
| 검증 결과 테이블 | `DataTable` | columns: [#, 여행명, 기간, 상태], sortable: true, 행 높이: 56dp |
| 성공 행 | `DataRow` | 배경: surface (#FFFFFF), trailing: Icons.check_circle (semanticSuccess) |
| 오류 행 | `DataRow` | 배경: semanticError 5% opacity, trailing: Icons.error (semanticError), 확장 시 에러 메시지 표시 |
| 결과 요약 | `Text` | "성공: N건" (semanticSuccess) + " / 오류: N건" (semanticError) |
| 오류 리포트 | `TextButton` | icon: Icons.download, text: "오류 리포트 다운로드", color: semanticError |
| 생성 버튼 | `ElevatedButton` | backgroundColor: b2bAccent (#1565C0), text: "일괄 생성 (N건)", style: Button_Primary |

#### 역할별 분기

해당 없음 (B2B 포털은 B2B 관리자 단일 역할).

#### 여행상태별 분기

해당 없음 (B2B 포털은 여행 상태에 무관하게 동일 동작).

#### 프라이버시등급별 분기

해당 없음 (B2B 포털은 프라이버시 등급에 무관하게 동일 동작).

#### 상태 분기

| 상태 | UI 변화 |
|------|---------|
| 초기 | 드롭존만 표시, 여행 기간 안내 Card_Alert 항상 표시, 업로드 상태/검증 결과 영역 숨김 |
| 파일 드래그 오버 | 드롭존 보더 색상 b2bAccent 진하게, 배경 b2bAccent 10% opacity |
| 업로드 중 | 드롭존 → 파일 정보 카드 전환, LinearProgressIndicator 진행 표시 |
| 업로드 완료 | 파싱 중 스피너 표시, "N건 파싱 중..." 텍스트 |
| 검증 완료 (오류 0) | 결과 테이블 전체 녹색, "전체 N건 성공" 메시지, 생성 버튼 활성 |
| 검증 완료 (오류 > 0) | 오류 행 빨강 하이라이트, 오류 리포트 다운로드 링크 표시, 생성 버튼 "일괄 생성 (성공건수만)" |
| 15일 초과 여행 검출 | 해당 행 오류 처리 + "여행 기간 15일 초과" 에러 메시지 (비즈니스원칙 §02.3) |
| 파일 형식 오류 | SnackBar "CSV 형식의 파일만 업로드할 수 있습니다." |
| 생성 진행 중 | 생성 버튼 → CircularProgressIndicator, "생성 중... N/M건" 진행률 |
| 생성 완료 | Dialog_Confirm "N건의 여행이 생성되었습니다." + N-01 이동 옵션 |
| B2B 계약 여행 수 초과 | Card_Alert "계약 여행 수 한도에 도달했습니다. 생성 가능: N건" (§12.2 `max_trips` 제한) |

#### 인터랙션

- [탭] 드롭존 → 파일 피커 (FilePicker, CSV 필터) → 파일 선택
- [드래그&드롭] CSV 파일 → 드롭존 → 업로드 시작 (웹 환경)
- [탭] 샘플 CSV 다운로드 → 브라우저 다운로드 (trip_template.csv)
- [탭] 오류 행 → 확장하여 상세 에러 메시지 표시
- [탭] 오류 리포트 다운로드 → CSV 형식 에러 리포트 다운로드
- [탭] 일괄 생성 → POST /api/v1/b2b/trips/bulk → 성공 시 완료 다이얼로그
- [탭] 도움말 (?) → Modal_Bottom CSV 형식 안내 (컬럼 설명, 날짜 형식, 15일 제한 등)
- [뒤로가기] → N-01 대시보드

---

### 2.3 [N-03] 대량 초대 코드

#### 메타데이터

| 항목 | 값 |
|------|-----|
| ID | N-03 |
| 화면명 | 대량 초대 코드 (Invite Bulk) |
| Phase | P3 |
| 역할 | B2B 관리자 |
| 진입 경로 | N-01 대시보드 → 빠른 실행 / 사이드바 → N-03 |
| 이탈 경로 | N-03 → N-01 (발송 완료 시) |

#### 레이아웃

```
┌─────────────────────────────────────┐
│ [←] 대량 초대 코드    [Tab1|Tab2]   │ AppBar (b2bAccent)
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────┐┌─────────────┐     │ Tab bar
│  │ 코드 생성     ││ 코드 상태     │     │ 2 tabs
│  └─────────────┘└─────────────┘     │
│                                     │
│  ═══════ [Tab1: 코드 생성] ═══════  │
│                                     │
│  초대 코드를 대량으로                │ headlineMedium (24sp, SemiBold)
│  생성하고 발송합니다                 │
│                                     │
│  ── Step 1: CSV 업로드 ──────       │ Step indicator (1/3)
│  ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐ │
│  ╎       📁                      ╎ │ Dropzone (dotted border)
│  ╎   CSV 파일을 선택하세요        ╎ │
│  ╎   (이름, 전화번호, 역할,       ╎ │ bodySmall, onSurfaceVariant
│  ╎    이메일(선택), 그룹명(선택)) ╎ │
│  ╎   [📥 샘플 CSV 다운로드]       ╎ │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘ │
│                                     │
│  ── Step 2: 역할 매핑 미리보기 ──   │ Step indicator (2/3)
│  ┌─────────────────────────────┐    │
│  │ 조직 유형: 학교 (수학여행)    │    │ 자동 감지 / 수동 선택
│  │                             │    │
│  │ CSV 역할  → SafeTrip 역할   │    │ 매핑 테이블 헤더
│  │ ─────────────────────────   │    │
│  │ teacher   → 캡틴 (인솔교사)  │    │ 자동 매핑 표시
│  │ student   → 크루 (학생)      │    │
│  │ parent    → 가디언 (학부모)  │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │ 미리보기 DataTable
│  │ # │ 이름    │전화번호   │역할 │    │
│  ├─────────────────────────────┤    │
│  │ 1 │ 김교사  │ 010-1234│캡틴 │    │ Badge_Role (teal)
│  │ 2 │ 이학생  │ 010-5678│크루 │    │ Badge_Role (gray)
│  │ 3 │ 박학부모│ 010-9012│가디언│    │ Badge_Role (green)
│  │ 4 │ 최학생  │ invalid │ ❌  │    │ 오류 행 (semanticError)
│  │...│         │         │    │    │
│  └─────────────────────────────┘    │
│  전체: 50명 / 유효: 48명 / 오류: 2명 │ bodyMedium
│                                     │
│  ── Step 3: 발송 방법 ───────────    │ Step indicator (3/3)
│  ┌─────────────────────────────┐    │
│  │ ○ SMS 발송                  │    │ RadioListTile
│  │ ○ 이메일 발송               │    │ RadioListTile
│  │ ● SMS + 이메일 동시 발송     │    │ RadioListTile (selected)
│  └─────────────────────────────┘    │
│                                     │
│  코드 유효 기간: [72시간 ▼]         │ DropdownButton
│                                     │
│  ── 발송 진행 ───────────────────    │
│  ┌─────────────────────────────┐    │
│  │ ████████████████░░  85%     │    │ LinearProgressIndicator
│  │ 41/48명 발송 완료            │    │ bodyMedium
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │  초대 코드 생성 및 발송 (48명) │    │ Button_Primary (b2bAccent)
│  └─────────────────────────────┘    │
│                                     │
│  ═══════ [Tab2: 코드 상태] ═══════  │
│                                     │
│  ── 배치 선택 ──────────────────    │
│  ┌─────────────────────────────┐    │
│  │ 2026 수학여행 3학년 2반 ▼    │    │ DropdownButton
│  └─────────────────────────────┘    │
│                                     │
│  ── 코드 상태 요약 ─────────────    │ Section header
│  ┌─────────────────────────────┐    │
│  │ 전체: 48건                   │    │ 요약 바
│  │ ████████░░░░░░░             │    │ SegmentedProgressBar
│  │ 📨 발송됨 10  👁️ 수신확인 8  │    │ (blue/teal/green/gray)
│  │ ✅ 사용됨 25  ⏳ 만료됨 5    │    │
│  └─────────────────────────────┘    │
│                                     │
│  ── 코드 상세 목록 ─────────────    │ Section header
│  ┌─────────────────────────────┐    │
│  │ # │이름    │코드    │상태   │    │ DataTable
│  ├─────────────────────────────┤    │
│  │ 1 │김교사  │A3B7X9K│✅사용됨│    │ Chip (green)
│  │ 2 │이학생  │K9M2P4H│📨발송됨│    │ Chip (blue)
│  │ 3 │박학부모│R5T8W3N│👁️수신 │    │ Chip (teal)
│  │ 4 │최학생  │J6Y1D2F│⏳만료됨│    │ Chip (gray)
│  │...│        │       │      │    │
│  └─────────────────────────────┘    │
│  ◀ 1 2 3 ... 5 ▶                   │ Pagination
│                                     │
│  ┌──────────────┐ ┌──────────────┐  │
│  │ 만료 건 재발송 │ │ 결과 다운로드 │  │ Button_Secondary x 2
│  └──────────────┘ └──────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

#### 컴포넌트 명세

| 컴포넌트 | Flutter 위젯 | 속성 |
|----------|-------------|------|
| 앱바 | `AppBar` | backgroundColor: b2bAccent (#1565C0), title: "대량 초대 코드", leading: BackButton |
| 탭 바 | `TabBar` | tabs: ["코드 생성", "코드 상태"], indicatorColor: b2bAccent |
| 제목 | `Text` | style: headlineMedium (24sp, SemiBold), color: onSurface |
| 스텝 인디케이터 | `Text` | style: titleMedium (18sp, SemiBold), "Step N:" prefix, b2bAccent 색상 |
| 드롭존 | `DottedBorder` + `GestureDetector` | border: dashed 2px b2bAccent, radius16, 높이: 140dp |
| 샘플 다운로드 | `TextButton` | icon: Icons.download, text: "샘플 CSV 다운로드", color: b2bAccent |
| 조직 유형 표시 | `Chip` | backgroundColor: b2bAccent 10%, text: 조직 유형명, textColor: b2bAccent |
| 역할 매핑 테이블 | `Card` + `Column` | Card_Standard, CSV 역할 → SafeTrip 역할 매핑 표시 (§01.4 기반) |
| 미리보기 테이블 | `DataTable` | columns: [#, 이름, 전화번호, 역할], 최대 10행 표시 + "더 보기" |
| 역할 뱃지 | `Container` (pill) | style: Badge_Role (캡틴 #00A2BD / 크루장 #015572 / 크루 #898989 / 가디언 #15A1A5) |
| 오류 행 | `DataRow` | 배경: semanticError 5% opacity, 역할 셀 빨강 텍스트 |
| 요약 텍스트 | `Text` | "전체: N명" (onSurface) + " / 유효: N명" (semanticSuccess) + " / 오류: N명" (semanticError) |
| 발송 방법 | `RadioListTile` x 3 | activeColor: b2bAccent (#1565C0), groupValue: deliveryMethod |
| 코드 유효 기간 | `DropdownButtonFormField` | items: [1시간, 24시간, 72시간(기본), 7일], value: 72시간 |
| 발송 진행 | `Card` + `LinearProgressIndicator` | progressColor: b2bAccent, 텍스트: "N/M명 발송 완료" |
| 발송 버튼 | `ElevatedButton` | backgroundColor: b2bAccent (#1565C0), text: "초대 코드 생성 및 발송 (N명)" |
| 배치 선택 | `DropdownButtonFormField` | items: 배치 목록 (batch_name), 최근 배치 우선 |
| 코드 상태 요약 | `Card` + `SegmentedProgressBar` | 4색: b2bAccent (발송됨) / primaryTeal (수신확인) / semanticSuccess (사용됨) / onSurfaceVariant (만료됨) |
| 코드 상세 테이블 | `DataTable` | columns: [#, 이름, 코드(7자리 모노스페이스), 상태], sortable: true, 페이지네이션 |
| 상태 칩 (발송됨) | `Chip` | backgroundColor: b2bAccent 10%, text: "발송됨", textColor: b2bAccent |
| 상태 칩 (수신확인) | `Chip` | backgroundColor: primaryTeal 10%, text: "수신확인", textColor: primaryTeal |
| 상태 칩 (사용됨) | `Chip` | backgroundColor: semanticSuccess 10%, text: "사용됨", textColor: semanticSuccess |
| 상태 칩 (만료됨) | `Chip` | backgroundColor: onSurfaceVariant 10%, text: "만료됨", textColor: onSurfaceVariant |
| 페이지네이션 | `Row` | 이전/다음 `IconButton` + 페이지 번호 `Text` |
| 재발송 버튼 | `OutlinedButton` | style: Button_Secondary, borderColor: b2bAccent, text: "만료 건 재발송" |
| 결과 다운로드 | `OutlinedButton` | style: Button_Secondary, icon: Icons.download, text: "결과 다운로드" |

#### 역할별 분기

해당 없음 (B2B 포털은 B2B 관리자 단일 역할).

#### 여행상태별 분기

해당 없음 (B2B 포털은 여행 상태에 무관하게 동일 동작).

#### 프라이버시등급별 분기

해당 없음 (B2B 포털은 프라이버시 등급에 무관하게 동일 동작).

#### 상태 분기

| 상태 | UI 변화 |
|------|---------|
| [Tab1] 초기 | Step 1 드롭존만 활성, Step 2~3 비활성 (opacity 0.4) |
| [Tab1] 파일 업로드 완료 | Step 1 완료 체크 (체크마크), Step 2 역할 매핑 + 미리보기 테이블 활성 |
| [Tab1] 역할 매핑 확인 | 조직 유형에 따른 자동 매핑 표시, 수동 변경 가능 (드롭다운) |
| [Tab1] 검증 완료 (오류 0) | 전체 유효, 발송 방법 선택 활성화 |
| [Tab1] 검증 완료 (오류 > 0) | 오류 행 빨강 하이라이트, "오류 제외 후 발송" 안내 |
| [Tab1] 발송 방법 선택 완료 | 발송 버튼 활성 (b2bAccent) |
| [Tab1] 발송 중 | 발송 버튼 비활성, 발송 진행 바 표시, 실시간 카운트 업데이트 |
| [Tab1] 발송 완료 | Dialog_Confirm "N명에게 초대 코드가 발송되었습니다." + Tab2 이동 안내 |
| [Tab1] 발송 일부 실패 | 결과 요약에 실패 건수 표시, "실패 목록 다운로드" 링크 |
| [Tab1] B2B 계약 만료 | Card_Alert "B2B 계약이 만료되었습니다. 코드 생성이 불가합니다." 발송 버튼 비활성 |
| [Tab2] 배치 미선택 | "배치를 선택하세요" 안내 |
| [Tab2] 배치 선택 완료 | 코드 상태 요약 바 + 코드 상세 테이블 표시, 10초 폴링으로 실시간 업데이트 |
| [Tab2] 전체 사용 완료 | 축하 배너 "전원이 초대코드를 사용하여 참여 완료했습니다" |
| [Tab2] 만료 코드 존재 | 재발송 버튼 활성, 만료 건수 secondaryAmber 강조 |

#### 인터랙션

- [탭] 탭 바 → 코드 생성 / 코드 상태 탭 전환
- [탭] 드롭존 → 파일 피커 (CSV 필터) → 파일 선택 → 자동 파싱
- [탭] 샘플 CSV 다운로드 → invite_template.csv 다운로드
- [탭] 조직 유형 변경 → 역할 매핑 테이블 갱신 (학교/여행사/기업 전환)
- [탭] 발송 방법 라디오 → 선택값 변경 (sms / email / both)
- [탭] 코드 유효 기간 드롭다운 → 유효 기간 설정 (초대코드_원칙 §03.2: 최소 1시간 ~ 최대 7일)
- [탭] 초대 코드 생성 및 발송 → POST /api/v1/b2b/batches → 배치 생성 + 코드 일괄 생성 + SMS/이메일 발송
- [탭] 오류 행 → 상세 에러 메시지 확장
- [Tab2] 배치 드롭다운 → 배치 선택 → GET /api/v1/b2b/batches/:batchId/status → 상태 표시
- [Tab2] 코드 상세 테이블 행 → Modal_Bottom 코드 상세 (코드 7자리, 발송 시각, 딥링크 URL, 상태 이력)
- [Tab2] 만료 건 재발송 → Dialog_Confirm "만료된 N건에 새 코드를 생성하여 재발송하시겠습니까?" → 확인 시 재발송
- [Tab2] 결과 다운로드 → CSV 형식 코드 상태 결과 다운로드
- [뒤로가기] → N-01 대시보드

---

## 3. 에러 & 엣지케이스

| # | 케이스 | 처리 방법 |
|---|--------|----------|
| 1 | CSV 파일 형식 오류 (컬럼 누락, 인코딩) | SnackBar "CSV 형식 오류: {상세 사유}". 오류 행 목록 반환 (초대코드_원칙 §12.2 ERR_CSV_FORMAT_INVALID) |
| 2 | CSV 행 내 전화번호 형식 오류 | 해당 행 빨강 하이라이트, "전화번호 형식이 올바르지 않습니다" 에러 메시지 |
| 3 | CSV 행 내 역할 값 오류 | 해당 행 빨강 하이라이트, "유효한 역할: crew_chief / crew / guardian" 안내 |
| 4 | 여행 기간 15일 초과 (N-02) | 해당 행 오류 처리, "여행 기간은 최대 15일입니다" 에러 (비즈니스원칙 §02.3) |
| 5 | B2B 계약 여행 수 한도 초과 (N-02) | Card_Alert "계약 여행 수 한도(max_trips)에 도달했습니다. 생성 가능: N건" (§12.2) |
| 6 | B2B 계약 만료 | 전체 생성/발송 기능 비활성, Card_Alert "계약이 만료되었습니다. 갱신을 요청해주세요." (ERR_B2B_CONTRACT_EXPIRED) |
| 7 | SMS/이메일 발송 일부 실패 | 실패 건수 표시 + "실패 목록 다운로드" 링크, 개별 재발송 가능 |
| 8 | 초대코드 만료 후 미사용 (N-03) | 코드 상태 대시보드에서 "만료됨" 표시, "만료 건 재발송" 버튼으로 새 코드 생성 |
| 9 | 동일 전화번호 중복 입력 | 중복 행 경고 하이라이트, "동일 전화번호가 N행에 중복됩니다" 안내 |
| 10 | 대량 업로드 파일 크기 초과 (10MB) | SnackBar "파일 크기가 10MB를 초과합니다. 파일을 분할해 주세요." |
| 11 | 네트워크 오류 (발송 중 연결 끊김) | 발송 진행 일시 중지, "인터넷 연결이 끊어졌습니다. 연결 복구 후 자동으로 재개됩니다." 안내 |
| 12 | 가디언 코드 수 초과 (B2B 계약 한도) | ERR_GUARDIAN_QUOTA_EXCEEDED, "B2B 계약에 명시된 가디언 수를 초과했습니다." (초대코드_원칙 §12.2) |

---

## 4. 오프라인 대응

| 상황 | 동작 |
|------|------|
| N-01 대시보드 접근 (오프라인) | 마지막 캐시된 통계 표시 + 상단 배너 "오프라인 상태입니다. 데이터가 최신이 아닐 수 있습니다." |
| N-02 CSV 업로드 (오프라인) | CSV 파일 선택은 가능하나 서버 파싱 불가, "인터넷 연결이 필요합니다" 안내 |
| N-02 일괄 생성 (오프라인) | 생성 버튼 비활성, "인터넷 연결이 필요합니다" 안내 |
| N-03 코드 생성 및 발송 (오프라인) | 발송 버튼 비활성, "인터넷 연결이 필요합니다. 연결 후 다시 시도해 주세요." 안내 |
| N-03 코드 상태 조회 (오프라인) | 마지막 캐시된 상태 표시 + "오프라인 상태입니다" 배너, 실시간 폴링 중단 |
| 오프라인 → 온라인 복구 | 자동 데이터 갱신 (N-01 통계, N-03 코드 상태), SnackBar "연결이 복구되었습니다." |

---

## 5. 검증 체크리스트

| # | 검증 항목 | 확인 |
|:-:|----------|:----:|
| 1 | N-01 대시보드에 초대코드 상태 현황 (발송됨/수신확인/사용됨/만료됨)이 표시된다 | |
| 2 | N-02 CSV 업로드 시 여행 기간 15일 초과 여행이 오류 처리된다 (§02.3) | |
| 3 | N-02 B2B 계약 여행 수 한도(max_trips) 초과 시 생성이 차단된다 (§12.2) | |
| 4 | N-03 CSV 역할 매핑이 조직 유형(학교/여행사/기업)에 따라 자동 적용된다 (§01.4) | |
| 5 | N-03 초대코드가 7자리 영숫자로 생성된다 (혼동 문자 O/0/I/l 제외) | |
| 6 | N-03 코드 유효 기간이 최소 1시간 ~ 최대 7일 범위로 설정 가능하다 (§03.2) | |
| 7 | N-03 코드 상태 대시보드에서 4가지 상태(발송됨/수신확인/사용됨/만료됨)가 정확히 추적된다 | |
| 8 | N-03 만료된 코드에 대해 새 코드 생성 후 재발송이 가능하다 | |
| 9 | N-03 SMS/이메일 발송 방법을 선택할 수 있다 (SMS/이메일/동시) | |
| 10 | 역할명이 캡틴/크루장/크루/가디언으로 정확히 표시된다 | |
| 11 | B2B 계약 만료 시 코드 생성/여행 생성 기능이 차단된다 (§12.2) | |
| 12 | CSV 파일 형식 오류 시 오류 행 번호와 사유가 목록으로 반환된다 | |
| 13 | 기준 문서 (비즈니스원칙 v5.1, 온보딩_UX_시나리오, 초대코드_원칙)가 명시되어 있다 | |
| 14 | 전체 3개 화면이 Phase P3으로 배치되어 있다 | |
| 15 | 에러 및 엣지케이스 처리가 포함되어 있다 | |
| 16 | 오프라인 대응이 포함되어 있다 | |

---

## 6. 변경 이력

| 버전 | 날짜 | 변경 내용 | 변경 사유 |
|------|------|----------|----------|
| v2.0 | 2026-03-04 | 전면 재작성: 8개 화면(N-01~N-08) → 3개 화면(N-01~N-03)으로 축소. N-04~N-08 제거 (Master_docs 미정의). 문서 템플릿을 새 형식(WF-B2B-N)으로 전환. N-01에 초대코드 상태 현황 추가, N-03에 코드 상태 대시보드 탭 통합, 역할 매핑 미리보기(§01.4) 추가 | Master_docs 기준 정합: 비즈니스원칙 v5.1 §12, 온보딩_UX_시나리오 §7, 초대코드_원칙 §09에 정의된 기능만 반영 |
| v1.0 | 2026-03-03 | 최초 작성 -- 8개 화면 (N-01 ~ N-08) 5-섹션 템플릿 | 초기 설계 |

---

## 7. 참조 테이블

| 문서 번호 | 문서명 | 관계 | 참조 섹션 |
|:--------:|--------|:----:|----------|
| #01 | SafeTrip_비즈니스_원칙_v5.1 | 상위 | §01.4 B2B 역할 매핑, §02.3 여행 기간 15일 제한, §12 B2B 프레임워크 |
| #07 | DB_설계_및_관계_v3_4 | 상위 | §4.45 TB_B2B_INVITE_BATCH, §4.6 TB_INVITE_CODE, TB_B2B_CONTRACT |
| #10 | 화면구성원칙 | 상위 | 와이어프레임 문서 계층 기준 |
| #14 | 온보딩_UX_시나리오 | 기준 | §7 시나리오 E (B2B CSV 일괄 등록 플로우) |
| #23 | 초대코드_원칙 | 기준 | §03 코드 생성 규칙 (7자리), §09 B2B 초대코드 (CSV, 배치, 상태 대시보드), §12 에러 처리 |
| 00 | 글로벌_스타일_가이드 | 참조 | 공통 컴포넌트, 시맨틱 컬러, 타이포그래피 |
